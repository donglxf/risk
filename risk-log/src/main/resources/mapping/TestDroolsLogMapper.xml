<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.ht.risk.log.mapper.TestDroolsLogMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="org.ht.risk.log.entity.TestDroolsLog">
        <id column="ID" property="id" />
        <result column="procinst_id" property="procinstId" />
        <result column="model_name" property="modelName" />
        <result column="sence_versionid" property="senceVersionid" />
        <result column="in_paramter" property="inParamter" />
        <result column="out_paramter" property="outParamter" />
        <result column="execute_total" property="executeTotal" />
        <result column="type" property="type" />
        <result column="create_time" property="createTime" />
        <result column="batch_id" property="batchId" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id as id, procinst_id as procinstid, model_name as modelname, sence_versionid as senceversionid, in_paramter as inparamter, out_paramter as outparamter, execute_total as executetotal, type as type, create_time as createtime,batch_id as batchid
    </sql>

    <select id="queryHitRuleByProcInstId" resultType="org.ht.risk.log.entity.HitRule">
        select d.version_id as senceVersionId,d.title as senceName,a.rule_name as ruleName,a.rule_desc as ruleDesc,c.execute_rulename as flag from risk_rule_his_version a
        inner join rule_scene_version d on a.sence_version_id = d.version_id
        inner join risk_test_drools_log b on a.sence_version_id = b.sence_versionid and d.version_id = b.sence_versionid
        left join risk_test_drools_detail_log c on a.rule_name = c.execute_rulename and b.id = c.drools_logid
        where b.procinst_id =  #{procInstId}
    </select>

    <select id="queryHitRuleByProcInstIds" resultType="org.ht.risk.log.entity.HitRule" parameterType="java.util.Map">
        select d.version_id as senceVersionId,d.title as senceName,a.RULE_NAME as ruleName,a.rule_desc as ruleDesc,sum(1) as flag from risk_rule_his_version a
        inner join rule_scene_version d on a.sence_version_id = d.version_id
        inner join risk_test_drools_log b on a.sence_version_id = b.sence_versionid and d.version_id = b.sence_versionid
        inner join risk_test_drools_detail_log c on a.rule_name = c.execute_rulename and b.ID = c.drools_logid
        where
        b.PROCINST_ID in
        <foreach item="procInstId" collection="procInstIds" separator="," open="(" close=")" index="">
            #{procInstId}
        </foreach>
        group by d.version_id,d.title,a.rule_desc,a.rule_name;
    </select>

</mapper>
